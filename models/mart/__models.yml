version: 2

models:
  - name: members
    description: Members of the Danish Parliament
    columns:
      - name: actor_id
        description: The unique key for each member.
        tests:
          - not_null
          - unique